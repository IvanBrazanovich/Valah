import {

} from "../types/index"


const initialState = {

    ingresos: [],
    gastos: [],
    meta: ""
}


export default function ( state = initialState, action ) {
    switch(action.type) {
        
        
        default:
            return state;
    }
}